describe('test', () => {
    // first test
    it('foo', () => {
        expect('foo').toEqual('foo');
    });

    // second test
    it('bar', () => {
        expect('bar').toEqual('bar');
    });
});
